#include "main.h"

/**
 * _abs - Entry point
 *
 * Description: computs absolute value on interger.
 *
 * @n: int argument to check
 *
 * Return: the absolute value of int
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);

	return (-n);
}
